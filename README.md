# 🌿 LinkStash

A warm, editorial-styled real-time bookmark manager. Save links privately, sync instantly across tabs, toggle between light and dark themes.

## 🔗 Setup

1. Clone and install:
```bash
npm install
```

2. Create `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
```

3. Run:
```bash
npm run dev
```

## 🛠 Stack
- Next.js 14 (App Router)
- Supabase (Auth + DB + Realtime)
- Tailwind CSS
- Playfair Display + Lato fonts

## 🎨 Design
Warm cream & forest green palette — light-first with a deep forest dark mode.
